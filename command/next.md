---
description: What should I work on now - frontier, due items, route to a goal
agent: cartographer
---

Plan the next move. Goal, if given: **$ARGUMENTS**

1. `due()` — anything decayed past threshold comes first. Review beats new
   material; a concept that lapses is a concept relearned from scratch.
2. `frontier(goal)` — what is learnable now, meaning every prerequisite is met.
3. If a goal was named, `path_to(goal)` for the ordered route and say how far
   off it is.
4. `status()` — if there are unresolved prerequisites, those are named-but-never-
   written concepts, and they are the honest next targets.

Answer in three parts: **Due now**, **Learnable now**, **Route**. Keep it to a
screen. Recommend one thing, not five.

The graph is only what the mentor has tested. An empty region means unexplored,
not unknown to him — he may know it perfectly well and simply never have been
tested on it. Never present a gap in the graph as a gap in his knowledge;
present it as something the mentor cannot vouch for.
